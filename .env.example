# Jano's QR Guest Locator Environment Variables
PORT=3000
ADMIN_PASSWORD=janos2026

# Supabase Configurations (Set these in Vercel / local .env file)
# If left empty, the application automatically falls back to local JSON file storage.
SUPABASE_URL=
SUPABASE_KEY=

# Resend Email Configuration
RESEND_API_KEY=
EMAIL_FROM=onboarding@resend.dev
